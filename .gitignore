# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.playwright-mcp
*storybook.log
storybook-static

# env
.env
.env.local
.env.production

# sidecar (Python/FastAPI) — never commit credentials or the local Telegram graph db
sidecar/.venv/
sidecar/__pycache__/
sidecar/**/__pycache__/
sidecar/*.session
sidecar/*.session-journal
sidecar/*.tgdb
sidecar/validation/volume_test.tgdb
sidecar/sidecar.log

# AI agent local/project guidance (keep CLAUDE.md only)
.claude/
.agents/
AGENTS.md

# Local working documents
*.pdf

# The pre-push name scan's input (scripts/scan-names.mjs). Committing this file would
# publish the exact strings the scan exists to keep out of the repo; the script refuses
# to run if git ever starts tracking it.
.scan-names
